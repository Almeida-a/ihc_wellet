import { TopbarSearchFilterPipe } from './topbar-search-filter.pipe';

describe('TopbarSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopbarSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
