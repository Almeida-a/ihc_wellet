import { Transaction, TransactionType, TransactionVisibility } from "src/utils/Transaction";
import { users } from "./UsersStorage";

export let incomes: Transaction[] = [
    //new Transaction(TransactionVisibility.VISIBLE, TransactionType.INCOME, users[0], "Monthly salary", 850.00)
]