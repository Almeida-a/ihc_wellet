import { Transaction, TransactionType, TransactionVisibility } from "src/utils/Transaction";
import { users } from "./UsersStorage";

export let expenses: Transaction[] = [
    //new Transaction(TransactionVisibility.VISIBLE, TransactionType.EXPENSE, users[0], "Dinner with friends", 25.60, "Food")
]